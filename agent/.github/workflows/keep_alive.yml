name: Keep Render Webhook Server Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping webhook server
        run: curl -sf https://greenguard-agent-tmw2.onrender.com/health
