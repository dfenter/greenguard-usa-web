Continue the Razorfin shark asset lane in play/razorfin/scratchpad/cline_sharks/ (read BRIEF.md, REPORT.md shortlist, NOTES-tear.md, fetch.sh, validate_glb.py, render_preview.py, shark_bake_local.py). Do NOT edit play/razorfin/tools/shark_bake.py; bake only with shark_bake_local.py (already patched: voxel-remesh high mesh first, then collapse). Check which species already have a .glb AND a _preview.png that you have visually inspected as intact (hammerhead is done and approved). MAKO PRIMARY (tubaf.vr, uid e4abb1...) is REJECTED: it is a museum jaw/head specimen, not a whole shark (see REJECTED_mako_tubaf_head_only_preview.png). Use the mako Alternates from REPORT.md instead (faerbogdan99 first, then arribadaclub). BEFORE baking any source, render the untouched download with render_preview.py and confirm it is a complete shark with body, fins and tail; reject and fall back if not. For each remaining species in order mako, tiger, thresher: download Primary via fetch.sh if src/<species>/ is missing (SKETCHFAB_API_TOKEN is in env, never print it), strip studio props if present, bake with shark_bake_local.py, validate with validate_glb.py, render with render_preview.py, LOOK at the preview (fins and tail intact, textures present). Long bakes exceed your 30s tool cap: launch detached writing to bake_<species>.log and poll. Never git commit. Finish by rewriting REPORT.md with full per-species results (source uid/author/license/URL, tris, textures, bones, flaws, regenerate command).
