You are Luna lane L2 of the Razorfin Rev 17 authored-families program. Read PLAN-rev17-families.md in this directory first (binding). Then read tools/shark_bake.py (reduce loop lines ~512-560, remesh fallback, orient helpers), NOTES-rev15-bake.md, hse/props_textured.js (what the procedural props tried to be: crown, horns, saw rostrum, hammer foil, spine plates; they are being REPLACED by real fused geometry).
YOU OWN ONLY: tools/sharklib/head.py, tools/sharklib/fins.py, tools/sharklib/accessories.py, tools/kit_gen.py, assets/kit/*.glb EXCEPT teeth_* (L1 owns teeth), NOTES-rev17-L2.md. L1 concurrently owns tools/sharklib/{io,bake,rig,export,mouth}.py and tools/shark_bake.py; L3 owns paint/finish/shark_variant.py/recipes. Until L1's io.measure exists, write your ops against a small local measure shim inside your own modules (same signature as the plan: long axis, L, zc, H, landmarks, fin vertex groups) and switch to sharklib.io when it lands (poll the file; do not edit it). Do not git commit.
Deliver, all as bpy functions taking a mesh object + params and returning the modified object, each runnable headless (/Applications/Blender.app/Contents/MacOS/Blender -b --python ... -- args): (1) tools/kit_gen.py: generate CC0 kit meshes from primitives and save assets/kit/crown_a.glb, horns_pair.glb, saw_rostrum.glb, spine_row.glb, plate_row.glb (Godzilla maple-leaf plates decreasing to tail), armor_collar.glb, hammer_foil.glb (flattened lobed T-head that wraps the snout, eye sockets at the tips), whale_hood.glb (broad flat filter-feeder head hood), each <= 600 tris, smooth-shaded, with a flat authored vertex colour. (2) head.py: lattice presets blunt/pointed/bulbous (4x4x4 lattice around the head box, control-point tables), graft(obj, other_base, neck_t=0.72) with ring resampling to 24 verts + bridge_loops + seam smoothing, foil/hood fuse via boolean union EXACT with fallbacks (holes_fill + remove_doubles first; EXACT -> FAST -> remesh-only fuse) then voxel remesh L/220 and smooth. (3) fins.py: select by vertex group, scale about the fin base ring, duplicate_along, extrude_tip (thresher whip), sail preset. (4) accessories.py: place kit GLBs at named landmarks with offset/scale/yaw, fuse, single voxel remesh if any fuse happened, decimate to a tri budget with the gentle-pass loop, and return a 'hi' duplicate that preserves the pre-op photo material + kit colours for L3's rebake. Verify each op on all four bases in assets/models/ (greatwhite_cy, thresher, tigershark, whaler): headless EEVEE renders to assets/review/L2_<op>_<base>.png (side + 3/4 view), tri counts before/after, loose-part count == 1 after fuse. Write NOTES-rev17-L2.md with commands, results, and the exact function signatures L3 should call. Finish with 'EXIT L2 PASS' or 'EXIT L2 PARTIAL <reason>'.
