You are Luna lane L1b (fix round) of the Razorfin Rev 17 authored-families program. Read PLAN-rev17-families.md, NOTES-rev17-L1.md, NOTES-rev17-L3.md, tools/sharklib/mouth.py, tools/sharklib/io.py, tools/sharklib/rig.py, tools/shark_variant.py (read-only) first.
YOU OWN ONLY: tools/sharklib/io.py, bake.py, rig.py, export.py, mouth.py, tools/kit_gen_teeth.py, assets/kit/teeth_*.glb, NOTES-rev17-L1.md. Other lanes are editing other files in this tree concurrently; never touch them. Do not git commit.
Orchestrator (Fable) review of the five pilot turntables in assets/review/*_turntable.png: THE MOUTH IS REJECTED on all five bases. Observed: the Cavity is a brown box hanging OUTSIDE the snout, and both tooth strips form a white comb sticking out past the nose. Root cause in mouth.py _make_cavity: front_y = nose + .006L and back_y = nose - cavity_depth*L places the wedge AT the nose tip in front of the body, and the body mesh is never bisected/opened, so nothing reads as a mouth. Also tigershark/whaler have no visible mouth opening at all and the leviathanrex jaw probe finds 0 lower-lip vertices.
Required outcome (the plan's mouth spec): the mouth must be INSIDE the head volume. Hinge at the measured jaw line (t 0.80..0.98 lower contour, jaw_z), lips at the snout. Do it this way: (a) subdivide the head region t>0.78; (b) bisect the BODY mesh with the two lip planes hinged at (0, ymin+0.80L, jaw_z): upper plane tilted +gape/2, lower plane tilted -gape/2 about local X; delete the wedge faces between the planes forward of the hinge (clear the wedge so there is a real opening); (c) build the cavity shell from the cut boundary loops: extrude the upper cut loop and lower cut loop inward toward the hinge and cap with the throat wall, material 'Cavity' dark rough; the cavity must be fully inside the original body bbox, back_y <= ymin+0.86L, and no cavity vertex may be forward of the nose landmark; (d) teeth strips: scale each strip to the cut loop width (measure the actual x-extent of the loop, not flank landmarks), place upper strip along the upper cut loop pointing DOWN, lower strip along the lower cut loop pointing UP, teeth must not protrude outside the lip silhouette by more than 0.01L; (e) keep the existing LowerJaw weight contract (lower lip + cavity floor + lower teeth = 1, smoothstep band, commissure .5/.5) and the rig.build_rig external payload path. Filter mode: wide low gape no fangs.
Acceptance gates you must run and print for ALL FOUR bases greatwhite_cy, thresher, tigershark, whaler (assets/models/*.glb) via tools/shark_bake.py --mouth path: cavity bbox inside body bbox; max cavity y <= nose.y; teeth max y <= nose.y + 0.01L; hse jaw probe (node --import ./tools/reg.mjs hse/probe_jaw.mjs <glb>) PASS with lower(n) >= 6 on every base; total added tris (cavity+teeth) <= 700. RENDER GATE (mandatory, no exceptions): Cycles CPU (scene.render.engine='CYCLES', samples 8, EEVEE aborts on this host) two-panel PNG per base, head close-up three-quarter view, LowerJaw at 0 and 25 deg, to assets/review/L1_mouth_<base>.png; then OPEN each PNG yourself (you can view images) and confirm in NOTES that the mouth reads as a mouth: a dark opening under the snout with teeth on the lip edges and nothing sticking out of the nose. Blender runs: /Applications/Blender.app/Contents/MacOS/Blender --gpu-backend opengl -b --factory-startup --python <script> -- args (one Blender at a time; other lanes may also be rendering, retry on abort). Write NOTES-rev17-L1.md (replace the mouth section; keep exact commands + gate numbers). Finish with 'EXIT L1b PASS' or 'EXIT L1b PARTIAL <reason>'.
