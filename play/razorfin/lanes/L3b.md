You are Luna lane L3b (fix round) of the Razorfin Rev 17 authored-families program. Read PLAN-rev17-families.md, NOTES-rev17-L3.md, NOTES-rev17-S3.md, tools/shark_variant.py, tools/sharklib/paint.py, finish.py, tools/recipes/*.json, hse/verify_families.mjs (read-only), shark3d.js lines 1086-1140 (read-only, how the game merges glTF primitives) first.
YOU OWN ONLY: tools/sharklib/paint.py, finish.py, tools/shark_variant.py, tools/recipes/*.json, assets/models/fam/*, assets/textures/rows/*, assets/review/*_turntable.png, NOTES-rev17-L3.md. L1b (mouth.py/rig.py/io.py) and L2b (head/fins/accessories) are fixing their modules concurrently in this tree; do not touch their files; re-read their NOTES-rev17-L1.md / L2.md before your final runs and adopt any signature changes they document. Do not git commit.
Orchestrator (Fable) review of your five turntables and assets/textures/rows/thresher.jpg: REJECTED on four counts you own.
1. PAINT is blown out to near-white on all five families (species_hsv v=.20-.38 recipes render as white sharks; thresher.jpg is grey/white with vertical white streaks). Fix the compose: the base colour must come from species_hsv (rendered body luminance target 0.35-0.55, the Rev-3D calibration), belly only below the measured centreline via the position map, and the photo detail term (photo/blur) must be multiplied in as a bounded modulation (0.75..1.25), never added. The vertical white streaks are UV-rasteriser gaps (uncovered texels); dilate covered texels outward 6px and fill the remainder with the family base colour, never white. Print per-family mean rendered luminance of the body texels and gate it to 0.30-0.60.
2. Thresher whip tip is a 3-unit needle (bbox y max 2.99 on an L=1 body): shark_variant.py maps the recipe 'scale' 2.2 straight into extrude_tip 'length' (fraction of L). Fix the mapping (whip length = fraction * scale bounded to <= 0.6L; final bbox long axis <= 1.6L) and update the recipe schema/docs.
3. Export: verify_families.mjs FAILS every family: 5 skinned meshes (body + Cavity + UpperTeeth + LowerTeeth + ...) and 9118 tris on aresrender/leviathanrex. Join the mouth objects into the body object before export (Blender join keeps vertex groups and materials; the exporter then emits one mesh with per-material primitives, which the game already merges); the S3 gate wants exactly 1 skinned mesh, bones exactly Tail3,Tail2,Tail1,Spine2,Spine1,Neck,Head,LowerJaw, 0 orphan weights, <=9000 tris, <=1MB. Pass accessories a tri_budget that leaves room for the mouth objects so the joined total is <= budget.
4. Turntable must be reviewable: 6 frames but the sharks are tiny. Frame each view so the shark fills ~80% of the frame width, add the mouth close-up at 25 deg as a real close-up (head fills the frame), neutral grey background (#3a4046), a 3-point light rig, and a text label per frame is not required. Keep Cycles CPU 8 samples.
Final deliverable: rerun all five recipes end to end (one Blender at a time; other lanes may be rendering, retry on abort), then run `node hse/verify_families.mjs` and `node --import ./tools/reg.mjs hse/probe_jaw.mjs assets/models/fam/*.glb` and paste both outputs in NOTES; both must PASS on all five. Then OPEN every assets/review/<family>_turntable.png yourself and write one honest sentence per family about whether it reads as an HSE-quality shark with a real mouth, correct paint and body-scale accessories; if a family fails on an L1b/L2b defect, say exactly which. Blender: /Applications/Blender.app/Contents/MacOS/Blender --gpu-backend opengl -b --factory-startup --python tools/shark_variant.py -- --recipe tools/recipes/<x>.json. Finish 'EXIT L3b PASS' or 'EXIT L3b PARTIAL <reason>'.
